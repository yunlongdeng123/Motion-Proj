diff --git a/scripts/colmap.py b/scripts/colmap.py
index 7affc68..bbb3932 100755
--- a/scripts/colmap.py
+++ b/scripts/colmap.py
@@ -22,6 +22,9 @@ def get_args():
     parser.add_argument('--use_gpu', action='store_true')
     parser.add_argument('--split_mode', default='nvs-75')
     parser.add_argument('--cam', type=int, default=1)
+    parser.add_argument(
+        '--num_threads', type=int, default=16,
+        help='COLMAP SIFT extraction/matching CPU thread limit')
     args = parser.parse_args()
     return args
 
@@ -101,23 +104,32 @@ def read_points3D_binary(path_to_model_file):
             errors[p_id] = error
     return xyzs, rgbs, errors
 
-def run_colmap(colmap_path, camera_meta, colmap_cmd='colmap', use_gpu=False, cam_num=1):
+def run_colmap(
+    colmap_path,
+    camera_meta,
+    colmap_cmd='colmap',
+    use_gpu=False,
+    cam_num=1,
+    num_threads=16,
+):
     mask_dir = os.path.join(colmap_path, 'masks')
     image_dir = os.path.join(colmap_path, 'images')
     assert os.path.exists(image_dir), 'Cannot find ' + image_dir
     assert os.path.exists(mask_dir), 'Cannot find ' + mask_dir
+    assert num_threads > 0, 'num_threads must be positive'
     print(image_dir)
 
     print_notice('Feature Extraction')
-    ret = os.system(
-        f'{colmap_cmd} feature_extractor \
+    feature_cmd = f'{colmap_cmd} feature_extractor \
         --ImageReader.mask_path {mask_dir} \
         --ImageReader.camera_model SIMPLE_PINHOLE  \
         --ImageReader.single_camera_per_folder 1 \
         --database_path {colmap_path}/database.db \
-        --image_path {image_dir}' + \
-        ' --SiftExtraction.use_gpu 0' if not use_gpu else ''
-    )
+        --image_path {image_dir} \
+        --SiftExtraction.num_threads {num_threads}'
+    if not use_gpu:
+        feature_cmd += ' --SiftExtraction.use_gpu 0'
+    ret = os.system(feature_cmd)
     assert ret == 0, 'There might be several mistakes in feature extraction.'
     print_notice('Feature Extraction Done')
 
@@ -153,11 +165,12 @@ def run_colmap(colmap_path, camera_meta, colmap_cmd='colmap', use_gpu=False, cam
     print_notice('Process camera meta Done')
 
     print_notice('Exhaustive Match')
-    ret = os.system(
-        f'{colmap_cmd} exhaustive_matcher \
-        --database_path {colmap_path}/database.db' + \
-        ' --SiftMatching.use_gpu 0' if not use_gpu else ""
-    )
+    matcher_cmd = f'{colmap_cmd} exhaustive_matcher \
+        --database_path {colmap_path}/database.db \
+        --SiftMatching.num_threads {num_threads}'
+    if not use_gpu:
+        matcher_cmd += ' --SiftMatching.use_gpu 0'
+    ret = os.system(matcher_cmd)
     assert ret == 0, 'There might be several mistakes in exhaustive match'
     print_notice('Exhaustive Match Done')
 
@@ -340,7 +353,14 @@ if __name__ == '__main__':
     else:
         assert False, 'Could not recognize scene type!'
 
-    run_colmap(colmap_path=colmap_dir, camera_meta=camera_meta, colmap_cmd=args.cmd, use_gpu=args.use_gpu, cam_num=args.cam)
+    run_colmap(
+        colmap_path=colmap_dir,
+        camera_meta=camera_meta,
+        colmap_cmd=args.cmd,
+        use_gpu=args.use_gpu,
+        cam_num=args.cam,
+        num_threads=args.num_threads,
+    )
     xyz, rgb, _ = read_points3D_binary(os.path.join(colmap_dir, 'triangulated/sparse/model/points3D.bin'))
     storePly(ply_path, xyz=xyz, rgb=rgb)
-    print('SfM pointcloud:', ply_path, 'pts:', xyz.shape[0])
\ No newline at end of file
+    print('SfM pointcloud:', ply_path, 'pts:', xyz.shape[0])
diff --git a/scripts/flow.py b/scripts/flow.py
index 570aae9..99a29a7 100755
--- a/scripts/flow.py
+++ b/scripts/flow.py
@@ -7,6 +7,7 @@ import torch.hub
 # LICENSE file in the root directory of this source tree.
 
 import os
+import sys
 import numpy as np
 import imageio
 import torch
@@ -800,14 +801,27 @@ if __name__ == '__main__':
     parser.add_argument('--cam', default=0, type=int)  # only use to generate video
     parser.add_argument('--fps', default=10, type=int)
     parser.add_argument('--split_mode', default='nvs-75')
+    parser.add_argument('--seed', default=0, type=int)
     args = parser.parse_args()
+    np.random.seed(args.seed)
+    torch.manual_seed(args.seed)
+    torch.cuda.manual_seed_all(args.seed)
     torch.cuda.set_device(args.device)
     src_path = args.path
 
-    cotracker = torch.hub.load("facebookresearch/co-tracker", "cotracker3_offline").cuda()
-
-    # for cached model
-    # cotracker = torch.hub.load("/root/.cache/torch/hub/facebookresearch_co-tracker_main", "cotracker3_offline", trust_repo=True, source='local').cuda()
+    cotracker_repo = os.environ.get(
+        "COTRACKER_REPO", "/root/autodl-tmp/third_party/co-tracker")
+    cotracker_checkpoint = os.environ.get(
+        "COTRACKER_CHECKPOINT",
+        "/root/autodl-tmp/checkpoints/cotracker3/scaled_offline.pth")
+    sys.path.insert(0, cotracker_repo)
+    from cotracker.predictor import CoTrackerPredictor
+    cotracker = CoTrackerPredictor(
+        checkpoint=cotracker_checkpoint,
+        offline=True,
+        window_len=60,
+        v2=False,
+    ).cuda()
 
 
     flow_video = None
@@ -837,4 +851,4 @@ if __name__ == '__main__':
         
     if flow_video is not None:
         imageio.mimwrite(flow_video_path, flow_video, fps=args.fps, quality=8)
-        print("Save a visualized video:", flow_video_path)
\ No newline at end of file
+        print("Save a visualized video:", flow_video_path)
diff --git a/scripts/run-dpt.py b/scripts/run-dpt.py
index c3080cd..03160fc 100644
--- a/scripts/run-dpt.py
+++ b/scripts/run-dpt.py
@@ -17,6 +17,9 @@ if __name__ == '__main__':
     parser.add_argument('--outdir', type=str, default='./vis_depth')
     
     parser.add_argument('--encoder', type=str, default='vitl', choices=['vits', 'vitb', 'vitl', 'vitg'])
+    parser.add_argument(
+        '--checkpoint',
+        default='/root/autodl-tmp/checkpoints/depth_anything_v2/depth_anything_v2_vitl.pth')
     
     parser.add_argument('--pred-only', dest='pred_only', action='store_true', help='only display the prediction')
     parser.add_argument('--grayscale', dest='grayscale', action='store_true', help='do not apply colorful palette')
@@ -33,7 +36,7 @@ if __name__ == '__main__':
     }
     
     depth_anything = DepthAnythingV2(**model_configs[args.encoder])
-    depth_anything.load_state_dict(torch.load(f'checkpoints/depth_anything_v2_{args.encoder}.pth', map_location='cpu'))
+    depth_anything.load_state_dict(torch.load(args.checkpoint, map_location='cpu'))
     depth_anything = depth_anything.to(DEVICE).eval()
     
     if os.path.isfile(args.img_path):
diff --git a/scripts/semantic.py b/scripts/semantic.py
index b1c4f70..f16c21d 100755
--- a/scripts/semantic.py
+++ b/scripts/semantic.py
@@ -1,5 +1,6 @@
 import os
 import argparse
+import shutil
 import torch
 import numpy as np
 import supervision as sv
@@ -13,7 +14,7 @@ from utils.common_utils import CommonUtils
 from utils.mask_dictionary_model import MaskDictionaryModel, ObjectInfo
 import json
 import copy
-os.environ['HF_ENDPOINT'] = 'https://hf-mirror.com'
+os.environ.setdefault('HF_ENDPOINT', 'https://hf-mirror.com')
 
 # mainly borrow from https://github.com/IDEA-Research/Grounded-SAM-2
 
@@ -27,6 +28,13 @@ def get_arguments():
     # parser.add_argument('--text', default='car.bus.truck.van.human.bike.')  # for nuScenes
     parser.add_argument('--name', default='semantic')
     parser.add_argument('--step', default=1, type=int)
+    parser.add_argument(
+        '--grounding_model_id',
+        default='IDEA-Research/grounding-dino-base')
+    parser.add_argument(
+        '--grounding_revision',
+        default='12bdfa3120f3e7ec7b434d90674b3396eccf88eb')
+    parser.add_argument('--work_dir', default=None)
     args = parser.parse_args()
     return args
 
@@ -60,9 +68,11 @@ image_predictor = SAM2ImagePredictor(sam2_image_model)
 
 
 # init grounding dino model from huggingface
-model_id = "IDEA-Research/grounding-dino-base"
-processor = AutoProcessor.from_pretrained(model_id)
-grounding_model = AutoModelForZeroShotObjectDetection.from_pretrained(model_id).to(device)
+model_id = args.grounding_model_id
+processor = AutoProcessor.from_pretrained(
+    model_id, revision=args.grounding_revision)
+grounding_model = AutoModelForZeroShotObjectDetection.from_pretrained(
+    model_id, revision=args.grounding_revision).to(device)
 
 
 # setup the input image and text prompt for SAM 2 and Grounding DINO
@@ -72,8 +82,9 @@ if text[-1] != '.':
     text = text + '.'
 
 # 'output_dir' is the directory to save the annotated frames
-output_dir = "./outputs"
-os.system('rm -r ' + output_dir)
+output_dir = args.work_dir or os.path.join(
+    args.path, ".adgs_semantic_work", args.name)
+shutil.rmtree(output_dir, ignore_errors=True)
 # 'output_video_path' is the path to save the final video
 # output_video_path = "./outputs/output.mp4"
 output_video_path = os.path.join(args.path, args.name + ".mp4")
@@ -91,8 +102,27 @@ frame_names = [
 ]
 frame_names.sort(key=lambda p: int(os.path.splitext(p)[0]))
 
+# Grounded-SAM-2 的 video loader 只按扩展名枚举 JPEG，而 AD-GS 的
+# nuScenes 预处理固定输出 PNG。为保持像素字节不变，在本次 work_dir
+# 中创建指向同一 PNG 内容的 .jpg 硬链接（跨文件系统时复制原始字节）。
+sam2_video_dir = video_dir
+if any(os.path.splitext(name)[-1].lower() == ".png" for name in frame_names):
+    stems = [os.path.splitext(name)[0] for name in frame_names]
+    if len(stems) != len(set(stems)):
+        raise RuntimeError("image 目录存在重复 frame stem，无法创建 SAM2 staging")
+    sam2_video_dir = os.path.join(output_dir, "sam2_frames")
+    os.makedirs(sam2_video_dir, exist_ok=True)
+    for frame_name, stem in zip(frame_names, stems):
+        source = os.path.join(video_dir, frame_name)
+        destination = os.path.join(sam2_video_dir, stem + ".jpg")
+        try:
+            os.link(source, destination)
+        except OSError:
+            shutil.copy2(source, destination)
+    print("SAM2 JPEG-extension staging:", sam2_video_dir, len(frame_names))
+
 # init video predictor state
-inference_state = video_predictor.init_state(video_path=video_dir, offload_video_to_cpu=True, async_loading_frames=True)
+inference_state = video_predictor.init_state(video_path=sam2_video_dir, offload_video_to_cpu=True, async_loading_frames=True)
 step = args.step # the step to sample frames for Grounding DINO predictor
 # step = len(frame_names) # the step to sample frames for Grounding DINO predictor
 
