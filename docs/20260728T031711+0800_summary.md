# 2026-07-28T03:17:11+08:00 工作结束 Summary

## 1. 当前结论

- 工作目录：`/root/autodl-tmp/motion_proj`
- VS Code / SSH 主机别名：`autodl`
- 计划入口：`docs/DYNAMIC_DRIVING_RECONSTRUCTION_PLAN_V1.md`
- 当前里程碑状态：
  - `DR-M2-ENV-ASSET-01`：完成。
  - `DR-M3-ADGS-0230-01`：完成，scene0230 的 60k 单场景基线及正式评估通过。
  - `DR-M4-ADGS-6SCENE-01`：阻塞。scene0242 的预处理和 train100 已完成，但官方 render100 连续两次达到 cgroup 内存上限的 90%，按预注册硬停规则终止。
- 结束时没有 AD-GS 训练、渲染、COLMAP 或预处理任务在运行；只保留 AutoDL 自带的 JupyterLab/TensorBoard 服务。
- 本轮没有执行 `git commit` 或 `git push`。

M4 当前不是代码异常或 CUDA OOM，而是外部资源条件不满足预注册执行合同。禁止在当前约 62 GiB cgroup 配额下放宽 90% 阈值、降低分辨率、减少相机、全局清缓存或原地覆盖证据后重跑。

## 2. 已完成的改动

### 2.1 M2 数据、环境和证据链

- 新增/更新数据构建与审计脚本：
  - `scripts/build_adgs_nuscenes_assets.py`
  - `scripts/audit_adgs_nuscenes_assets.py`
  - `scripts/audit_adgs_processed_scene.py`
  - `scripts/finalize_dr_m2_evidence.py`
- 新增/更新环境报告与 smoke test：
  - `scripts/env_report_adgs.sh`
  - `scripts/env_report_all.sh`
  - `scripts/smoke_adgs_env.py`
  - `scripts/smoke_cotracker_env.py`
  - `scripts/smoke_dpt_env.py`
  - `scripts/smoke_grounding_dino_hf.py`
  - `scripts/smoke_sam_env.py`
- `envs/` 中保留 AD-GS conda/pip 锁定信息、构建日志和安装日志。
- 资产构建现在会复制并校验 nuScenes map masks，也会复用已经完整的 sensor payload。
- Grounding DINO Hugging Face smoke test 使用固定 revision，避免浮动依赖。

通过的 M2 实例：

`/root/autodl-tmp/runs/dynamic_recon/DR-M2-ENV-ASSET-01/20260727T180733__e49a4e-4080s-r3`

关键结果：

- 1440/1440 sensor 文件完整。
- 4/4 map masks 完整。
- 6 个目标场景均为 180 张 RGB + 60 帧 lidar。
- 环境、资产和 smoke audit 全部通过，terminal status 为 `done`，返回码 0。
- data manifest SHA256：`64c68972a25834757168cd8fdc11c64b134b6ae0d9206a9ebde4064891c16092`
- 峰值 cgroup 内存：30,123,261,952 bytes；无 OOM。

保留的失败实例：

`/root/autodl-tmp/runs/dynamic_recon/DR-M2-ENV-ASSET-01/20260727T165549__e49a4e-4080s-r2`

该实例因缺少 4 个 map masks 正确标记为 `blocked`，没有覆盖或删除。

### 2.2 AD-GS 兼容性补丁

补丁：

`compatibility/AD-GS-2026-07-27.patch`

SHA256：

`49b4c06ecec6c30f1e80b5abf4d46970920f9d71952acbda273774d9b5b34f48`

补丁包含：

- `flow.py`：固定随机种子，并使用本地锁定的 CoTracker 仓库与 checkpoint。
- `run-dpt.py`：显式指定 checkpoint。
- `semantic.py`：锁定 Grounding DINO revision；使用 run-local 工作目录；为只枚举 JPEG 的 SAM2 loader 建立字节不变的 `.jpg` hardlink/copy staging。
- `colmap.py`：SIFT extraction/matching 显式使用 `--num_threads`，默认 16，避免 COLMAP 默认 128 线程造成 cgroup 内存尖峰。

补丁反向应用检查通过。上述改动没有修改模型、损失函数、指标定义、数据划分或正式评估协议。

### 2.3 运行器

- `scripts/run_dr_m3_scene0230.py`：作为 M3 scene0230 的冻结执行器保留。
- `scripts/run_dr_adgs_scene.py`：M4 通用六场景执行器，只参数化正式六场景中的 `--scene` 与 `--task-id`，同时阻止跨场景复用。
- 通用执行器 SHA256：`b6d91f6986828dc8d23d0a6384bfe5bc5f9c3f466f91c189f167b0d2e8d52be8`
- Python 语法、帮助输出、scene0242 dry configuration 以及 `git diff --check` 均已通过。

### 2.4 研究文档

已更新：

- `docs/DYNAMIC_DRIVING_RECONSTRUCTION_PLAN_V1.md`
- `docs/EXPERIMENTS.md`
- `docs/RESEARCH_FAILURES.md`
- `docs/RESEARCH_STATUS.md`

文档已记录 M2/M3 结果、M4 scene0242 证据、恢复条件，以及以下失败条目：

- `PIVOT-F10`：PNG/JPEG loader 契约不匹配。
- `PIVOT-F11`：COLMAP 默认 128 线程导致内存风险。
- `PIVOT-F12`：M4 官方渲染触发 cgroup 90% 预注册硬停。

## 3. M3 scene0230 正式结果

最终实例：

`/root/autodl-tmp/runs/dynamic_recon/DR-M3-ADGS-0230-01/20260727T195611__scene0230__s0-r3`

该实例复用了 r2 中 860 个预 COLMAP 文件，并通过逐文件哈希验证；复用 fingerprint：

`836b8f1480b083e5d9180f235e06d290c2ed7c5eb6f4de791d920fe945282891`

数据与 COLMAP：

- 180 张 image/depth/sky/semantic。
- 光流 138/138。
- COLMAP 注册 138/138，70,933 个稀疏点，重投影误差 1.09244 px。
- 主点云 332,805 点。

正式 60k test：

- SSIM：0.9053635512
- PSNR：29.9026953833
- LPIPS(VGG)：0.2121784229
- LPIPS(ALEX)：0.1123770318
- FPS：48.08876

正式 60k train：

- SSIM：0.9392803527
- PSNR：33.6398032921
- LPIPS(VGG)：0.1813450340
- LPIPS(ALEX)：0.0918728958
- FPS：42.33252

最终点数：

- 总计 1,315,757
- scene 1,203,004
- object 112,753

资源：

- train 峰值 cgroup：59,136,491,520 bytes；GPU 峰值 16,039 MB。
- render 峰值 cgroup：59,530,678,272 bytes；GPU 峰值 6,407 MB。
- 无 OOM。
- render 峰值达到内存配额的 89.423%，距离预注册 90% 硬停仅约 384 MB。

最终 audit：

`/root/autodl-tmp/runs/dynamic_recon/DR-M3-ADGS-0230-01/20260727T195611__scene0230__s0-r3/m3_final_audit.json`

SHA256：

`003d36a5fb85c6fadeeaeb0bdea975e1ed872b8eaa2adf5131f6411acf5c9eec`

所有 train/test render 和 GT 均为 1600×900、非空、非黑图。人工检查 test 00000/00021/00041 时，视角和静态几何一致；动态车辆及标志存在模糊/重影，这是当前基线局限。M3 没有预注册的单场景数值门槛，因此不能把该结果表述为已经完成六场景论文复现。

保留的 M3 失败证据：

- r1：PNG/JPEG loader 契约问题。
  `/root/autodl-tmp/runs/dynamic_recon/DR-M3-ADGS-0230-01/20260727T181617__scene0230__s0`
- r2：COLMAP 默认 128 线程造成资源风险。
  `/root/autodl-tmp/runs/dynamic_recon/DR-M3-ADGS-0230-01/20260727T182247__scene0230__s0-r2`

对应失败 processed 数据分别保存在：

- `/root/autodl-tmp/data/dynamic_recon/processed_failed/20260727T181617__scene0230__s0`
- `/root/autodl-tmp/data/dynamic_recon/processed_failed/20260727T182247__scene0230__s0-r2`

## 4. M4 scene0242 进展与硬阻塞

实例：

`/root/autodl-tmp/runs/dynamic_recon/DR-M4-ADGS-6SCENE-01/20260727T235743__scene0242__s0`

manifest fingerprint：

`496590eed7e97689bd07e241e612adccee9d5854980eb2952bb1871fac9cf7c2`

已完成：

- 180 张 image/depth/sky/semantic。
- 光流 138/138。
- 主 lidar 点云 392,177 点。
- COLMAP 注册 138/138；1,639 个 SfM 点；9,306 observations；平均 track length 5.677852；重投影误差 1.687979 px。
- train100 完成：
  - test PSNR：16.8178507487
  - train PSNR：17.16220665
  - 总点数：79,320
  - scene 点：17,822
  - object 点：61,498
  - 峰值 cgroup：59,359,428,608 bytes
  - GPU 峰值：6,625 MB

阻塞发生在官方 render100。只渲染到 2/138 时，内存连续两个采样达到或超过预注册的 90% 阈值：

- 峰值 cgroup：59,996,393,472 bytes
- 90% 阈值约：59,914,793,779 bytes
- 超出约：81,599,693 bytes
- stage 返回码：`-15`（SIGTERM，由运行器主动硬停）
- terminal status：`blocked`
- 结束时间：`2026-07-28T03:12:09+08:00`
- 无 `oom` / `oom_kill`

COLMAP 点数较少是真实场景难度，但注册覆盖、结构审计和 lidar 主点云均通过，因此没有篡改或丢弃该结果。

## 5. 下一步待办

1. 先调整外部资源：将 cgroup 内存提高到至少 80 GiB，建议 96 GiB，为 60k 训练、官方全量渲染和监控留出余量。
2. 资源调整后，为 scene0242 创建新的 M4 实例，不覆盖当前 blocked 实例。
3. 在恢复前，为已经完成的 scene0242 processed 数据增加或使用“完整预处理采用”路径，并对来源、文件数、哈希和 manifest 做显式审计；不要让通用运行器无意覆盖现有 processed 数据。
4. 先完成 scene0242 的 train100/render100/eval100、train1000/render1000/eval1000、train60000/render60000/eval60000。
5. scene0242 通过后，再串行运行 scene0255、scene0295、scene0518、scene0749；scene0230 使用已冻结的 M3 正式结果。
6. 汇总六场景 mean、worst、coverage，并对预注册门槛判定：
   - PSNR ≥ 30.56
   - SSIM ≥ 0.915
   - LPIPS ≤ 0.184
7. 只有 M4 完成后，才继续计划中的 M5 及后续里程碑。

如果无法提升内存，必须先修改实验计划并登记为新的协议版本；不能把降低分辨率、减少相机、放宽硬停阈值或只报告部分场景混入当前 M4 证据。

## 6. 交接现场

- 项目 Git HEAD：`2d46f4c1c79708451081f291a267a7acd26a3236`
- 工作树包含本轮文档、脚本、环境记录与兼容性补丁，尚未提交。
- 远程磁盘结束前约使用 110 GiB，剩余约 141 GiB（44% 已用）。
- raw subset 约 2.9 GiB。
- scene0230 / scene0242 processed 各约 6.4 GiB。
- M3 最终 run 约 4.4 GiB。
- M4 blocked run 约 1.4 GiB。
- `memory.events` 中 `oom=0`、`oom_kill=0`。

建议另一台设备连接后先执行：

```bash
ssh autodl
cd /root/autodl-tmp/motion_proj
sed -n '1,260p' docs/20260728T031711+0800_summary.md
git status --short
```

然后检查 M4 terminal 与监控证据，再决定是否已经完成 cgroup 扩容：

```bash
cat /root/autodl-tmp/runs/dynamic_recon/DR-M4-ADGS-6SCENE-01/20260727T235743__scene0242__s0/terminal.json
cat /sys/fs/cgroup/memory.max
cat /sys/fs/cgroup/memory.events
```
