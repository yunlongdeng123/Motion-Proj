# WorldSim V6.3 P0 Scope Freeze

- Task: `WS-V63-P0-SCOPE-GIT-01`
- Status: `done`
- Branch: `research/worldsim-v6.3-surface-tail`
- Upstream: V6.2 closed negative, recovery exhausted, P7/P8 not unlocked
- Default resource: one RTX 3090 24GB

## Frozen scientific boundary

V6.3 does not reopen CPSC-Lite. The method family must combine native IR-WM logits/latent, proposal-native surface topology,
exact hard evidence projection, patch/proposal tail risk, positive OCC authority and independent case-level calibration.

Disallowed: prototype/fabricated native features, legacy O_eval tuning, per-voxel pseudo-independent calibration, mean query risk as
the primary safety statistic, all-UNKNOWN as success, changing the denominator after quality read, or adding hashes, checksums or
fingerprints. Git commit history and semantic run IDs remain ordinary source-control/provenance records, not artifact validation.

## Stage order and stop boundary

P1 freezes novelty and protocol before quality. P2 materializes native sidecars, P2D diagnoses native pointwise transfer, P3 builds
the surface corpus, P4 probes capacity, P5 trains, P6 runs fresh matched AB, P7 calibrates, P8 audits legacy28, and only passing frozen
gates can unlock P9/P10. Stop rules close a failed family without sweeps. A faithful resource failure follows the plan's bounded
single-recovery contract and is reported as `blocked_resource`, not algorithm rejection.

## P0 audit

V6.2 was fast-forward integrated into `main`; its targeted projection test passed under the correct repository import contract.
The V6.3 branch was created from that synchronized main. GPU and disk resources were sufficient. No legacy, confirmation or test
quality was read. Failure ledger delta: `V63-F01 resolved`.
