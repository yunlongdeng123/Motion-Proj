# V6.7 P18 selective authority freeze

P18 freezes qmean as the action reliability ordering. It cannot change which bottom-quartile actions are preferred inside a case.
A seven-feature monotone benefit head sees only the observable qmean selection margin, dispersion, selected corridor uncertainty and
visited support. It predicts when the frozen ordering should receive authority; other cases abstain.

P10V and P10X remain two development domains. P9 is already consumed by P16/P17 and is explicitly method selection, not fresh
confirmation. The within-case action fraction is 0.25 and authority coverage is fixed at 0.50 before training. Gates require at
least 0.49 coverage, 0.45 authorized selected-cost reduction, +0.02 over ungated qmean, and five non-increasing scenes.

There is one GPU run with no feature, loss, coverage or gate sweep. This is selective task authority only; it does not assert
conformal exchangeability, population guarantees, collision avoidance, planning policy, closed loop or safety.
