# 当前研究状态

更新：2026-09-20。分支：`research/worldsim-v7.4-h2-generative-surface`。本文件是唯一当前快照；历史见[V7.4归档](archive/2026-09/v74-0920/STATUS_PRE_V75.md)，实验按[索引](EXPERIMENTS.md)查阅。

## 当前方向与授权

按用户最新要求，V7.5研究 **Which reconstruction errors matter to the generative world model state?**：通过生成观测→策略→动作→ego/相机→下一段生成的实际反馈，判断哪些重建状态误差影响驾驶任务。停止追加长时间放大和局部定位诊断，不以证明放大作为接入闭环的前提。定义、架构与停止条件见[PROBLEM](v75/PROBLEM.md)。

单张RTX3090正式推理已获授权；慢可以接受，任何OOM立即停止，不自动降配置、重试或调研多卡。无训练、旧队列恢复或关机任务。模型与样例资源就绪，下载heartbeat已暂停。

## 已完成证据

- [单卡基线](v75/BASELINE.md)：官方单视图2B，704×1280、237帧，完整重复237/237帧一致；PyTorch峰值12.81GiB，旧整卡采样峰值17.41GiB，无OOM。另完成三个[原生开发样例](v75/NATIVE_COHORT.md)与[AV2输入桥接](v75/AV2_BRIDGE.md)。保留样例没有用于调参。
- [定位实验](v75/LOCALIZATION.md)：一个场景两seed十组，恢复条件后二维差下降，主要响应与普通投影同量级。无稳定额外放大证据，不追加长时追查。
- [自然读出](v75/NATURAL_STATE.md)：八日志筛查、四例DVGT读出保留好坏表现；2.965m候选两seed存在二维条件响应，但普通框三维更差也可生成更好的二维指标。后段固定点支持不足，不能晋级稳定米制漂移。其后四日志窗口4→3→0，真实遮挡排除，无重建/生成、不补位。
- [可见性窗口](v75/VISIBLE_COHORT.md)：4日志→8候选→2真实可测目标→2次DVGT→1参考合格目标→4组生成，全部结束。银色轿车3.423m残余未统一退化；LiDAR改善检测中心3.78→1.74px，共同点2.82→3.37px，不能声称一致修复。黑色车辆参考0.663m未过旧0.5m规则，不生成不替换。两例全局尺度均改善，保留强控制。
- 四视频各237帧全部解码；检测19/20，普通框缺一次，完整均值留空。条件刚体比较四组共同点为0，米制排名为空；请求相机不等于已验证生成相机。额外RAFT检查完成、无OOM，没有新增failure卡。
- 工程失败[V75-F01](research_failures/entries/V75-F01.md)已绕过：显式绑定底层官方pipeline输入；第三方源码未修改。人工verdict均为null。

## 当前工作与缺口

[首轮跟车反馈](v75/FOLLOWING_CLOSED_LOOP.md)已全部完成：6条已有开发日志筛查，3条持续路径前车任务、2条真实前视角基线合格，选首条0bae3b5e运行4组。另1条GT前车在前视相机之外，属于输入不可观测，不记检测器失败。四组各117帧/15次决策，无OOM；60次决策时序与468帧参考检查通过。

GT生成任务基线通过，路线横向偏差最大0.129m、在线加速度相对oracle差中位0.038m/s²。自然DVGT终点相对GT条件差0.353m，普通全局尺度0.242m，目标LiDAR修复0.104m；对应初始中心残余2.965/4.769/0.417m。四组都没有制动或参考重叠，保留为小幅、可部分恢复的闭环响应，不能称作严重驾驶badcase。普通尺度的几何残余更大但任务差更小，不能按3D范数直接排序危害。

反馈使用单前视角感知＋官方nuPlan IDM及已知路线/地图地面，属于有限普通控制任务。默认虚拟ego、平地、非反应式记录traffic，没有完整驾驶规则或跨模型结论。全部当前推理/评价队列已结束，无训练、下载或电源控制。

[制动任务窗口](v75/BRAKING_TASKS.md)已完成：固定12条本地完整日志、每条4起点，48→9个记录减速窗口→0个原持续前车任务合格。唯一有明确前车制动需求的02678d04 +6.5s，起始目标45.124m、随后进入40m范围，被“起点就有40m内前车”的入口排除。原终态保留，没有改门槛重算或新增来源补位。

对此仅做一次明示事后的真实RGB入口诊断：5次检测，4/5范围前车判断相符，距离误差中位9.845m，超过既有3m状态基线要求；加速度差中位却只有0.302m/s²。同一IDM的离线GT/RGB状态组合证明距离偏小和前车速度偏大会部分抵消，动作接近不能证明状态正确。这是普通真实RGB基线边界，不是生成模型badcase；0次新重建、0次新生成、无OOM，人工verdict为null。

下一步先处理已有接近任务的输入适用性与普通状态读出，再比较路径占用、相对距离/速度及进入路径时机的联合影响。GT度量替换单列诊断，不悄悄替代RGB策略。不要以更极端来源、seed、扰动或时长维持失败主张；不继续长时间放大和局部定位诊断。当前全部调用已结束、GPU空闲，无训练、下载或电源控制。

不恢复已失败的TransFuser域基线或以代理指标冒称闭环；保留V7.4 [F20](research_failures/entries/V74-H2-F20.md)、[F21](research_failures/entries/V74-H2-F21.md)、[F22](research_failures/entries/V74-H2-F22.md)边界。没有合格跨场景生成闭环badcase，没有有效主方法或普遍SOTA失效证据。研究目标继续推进。
