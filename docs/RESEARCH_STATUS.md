# 当前研究状态

更新：2026-09-20。分支：`research/worldsim-v7.4-h2-generative-surface`。本文件是唯一当前快照；历史见[V7.4归档](archive/2026-09/v74-0920/STATUS_PRE_V75.md)，实验按[索引](EXPERIMENTS.md)查阅。

## 当前方向与授权

按用户最新要求，V7.5研究 **Which reconstruction errors matter to the generative world model state?**：通过生成观测→策略→动作→ego/相机→下一段生成的实际反馈，判断哪些重建状态误差影响驾驶任务。停止追加长时间放大和局部定位诊断，不以证明放大作为接入闭环的前提。定义、架构与停止条件见[PROBLEM](v75/PROBLEM.md)。

单张RTX3090正式推理已获授权；慢可以接受，任何OOM立即停止，不自动降配置、重试或调研多卡。无训练、旧队列恢复或关机任务。模型与样例资源就绪，下载heartbeat已暂停。

## 已完成证据

- [单卡基线](v75/BASELINE.md)：官方单视图2B，704×1280、237帧，完整重复237/237帧一致；PyTorch峰值12.81GiB，旧整卡采样峰值17.41GiB，无OOM。另完成三个[原生开发样例](v75/NATIVE_COHORT.md)与[AV2输入桥接](v75/AV2_BRIDGE.md)。保留样例没有用于调参。
- [定位实验](v75/LOCALIZATION.md)：一个场景两seed十组，恢复条件后二维差下降，主要响应与普通投影同量级。无稳定额外放大证据，不追加长时追查。
- [自然读出](v75/NATURAL_STATE.md)：八日志筛查、四例DVGT读出保留好坏表现；2.965m候选两seed存在二维条件响应，但普通框三维更差也可生成更好的二维指标。后段固定点支持不足，不能晋级稳定米制漂移。其后四日志窗口4→3→0，真实遮挡排除，无重建/生成、不补位。
- [可见性窗口](v75/VISIBLE_COHORT.md)：4日志→8候选→2真实可测目标→2次DVGT→1参考合格目标→4组生成，全部结束。银色轿车3.423m残余未统一退化；LiDAR改善检测中心3.78→1.74px，共同点2.82→3.37px，不能声称一致修复。黑色车辆参考0.663m未过旧0.5m规则，不生成不替换。两例全局尺度均改善，保留强控制。
- 四视频各237帧全部解码；检测19/20，普通框缺一次，完整均值留空。条件刚体比较四组共同点为0，米制排名为空；请求相机不等于已验证生成相机。额外RAFT检查完成、无OOM，没有新增failure卡。
- 工程失败[V75-F01](research_failures/entries/V75-F01.md)已绕过：显式绑定底层官方pipeline输入；第三方源码未修改。人工verdict均为null。

## 当前工作与缺口

已接通生成RGB→感知/跟踪→固定IDM→官方ego动力学→下一段生成的真实反馈，真实RGB和GT生成分别门控。结果与架构集中见[接近任务报告](v75/APPROACH_CLOSED_LOOP.md)；保存路线、标定、官方地形、其他演员及非反应式参考轨迹等信息边界，没有把受控适配器称为原生端到端重建排名。

**形状主候选已关闭，当前完成原生DVGT输入范围的有限检查。** 上轮`WS-V75-SHAPE-CONFIRM-01`的唯一额外seed43使02678d04配对行进差从+2.757m变为−0.324m，平均动作误差和最大欠制动差也反向；两个GT基线通过，所有分支保留，不追加seed44或更严重来源。

24642607保留一个小幅、两seed同向且普通先验可恢复的现象：配对行进差+0.301/+0.257m、平均动作误差差+0.123/+0.119m/s²。类别先验两seed均通过原绝对门控；可见拟合的动作误差中位数0.557/0.532m/s²，略高于原0.5门槛。类别先验令其降至0.312/0.306，欠制动略降、额外制动不变，行进也接近各自GT。承认这个普通解，但一个已曝光任务的小效应不足以支持严重驾驶危害、普遍SOTA失效或开发复杂主方法。

此前边界继续有效：两个接近任务的中心/全局尺度/目标LiDAR没有同时恢复动作与执行；有限IoU关联未全面恢复；时间审计确认目标近静止；固定6日志×3起点的运动跟车窗口18→0。相应平移、尺度、关联、运动、时长与seed搜索均关闭。两任务/两seed的全部正反结果与原失败记录保留，不用小效应替换预先指定但失败的主候选。

`WS-V75-DVGT-CONTRACT-01`的CPU检查确认旧官方预处理像素完全一致，T=1没有执行点头分块，0.1单位换算/RDF坐标和校准往返代数一致。旧距离适配会丢掉原生点图方向误差，不能当作完整三维状态重建排名；未重复PnP或拟合新坐标。详细契约、模块图与全部指标见[自然状态报告](v75/NATURAL_STATE.md#原生点图契约与有限时间上下文控制)。

随后`WS-V75-DVGT-TEMPORAL-CONTRACT-01 / 20260920-r1`仅做两次固定三时刻/2Hz官方前向。最后一帧与旧输入逐像素相同，全部RGB截止前可用。第一例前视角误差97.68°→4.01°，正深度点26.4%→100%；14视角中13个改善，一个变差。普通历史观测解释了最严重错位的大部分，不再把单时刻巨大误差当稳定代表。三帧残余3.21°–11.37°，两例均未通过事前原生投影筛查；异步捕获与数据域限制尚未分离，没有证据将全部残余归为模型固有缺陷。

两次推理合计34.53秒（含加载/保存），峰值16.770GiB，无OOM；本轮0次世界模型生成。完整输入、原生数组、输出在`/root/autodl-tmp/runs/worldsim_v75/WS-V75-DVGT-TEMPORAL-CONTRACT-01/20260920-r1/`，轻量协议/结果/科学图归档至`docs/autoresearch/worldsim_v75/native_contract/`。GPU任务已结束，无训练、下载或电源控制。

**下一步回到任务影响的资格判断，关闭本轮接口搜索。** 不追加时间长度、seed、相机顺序或拟合变换，不以原生方向残余直接登记新的生成badcase。现有证据只能支持“一例小幅形状条件响应可由普通类别先验改善”；中心/尺度与较大形状候选没有可重复的一致恢复。尚缺可靠自然对象状态到生成/策略执行的完整对照，以及完整合法输入的成熟驾驶基线；在这两处存在实质进展前，不扩展模型或训练。下轮先汇集已完成的真实反馈与强控制，明确任务损失发生在哪个接口及普通办法已解决多少，再决定是否存在新的、可证伪的剩余问题。

同一本地审阅页`outputs/V75_Multiscene_Closed_Loop/index.html#native-contract`保留发现、复核、正反案例与本轮原生检查。人工verdict均null，`failure_ledger_delta:none`。保留V7.4 [F20](research_failures/entries/V74-H2-F20.md)、[F21](research_failures/entries/V74-H2-F21.md)、[F22](research_failures/entries/V74-H2-F22.md)边界，不恢复失败的TransFuser域基线。尚未确认值得推进复杂主方法的普遍或有实质影响的可恢复缺口，研究目标继续推进。
