# 当前研究状态

更新：2026-09-20。分支：`research/worldsim-v7.4-h2-generative-surface`。本文件是唯一当前快照；历史见[V7.4归档](archive/2026-09/v74-0920/STATUS_PRE_V75.md)，实验按[索引](EXPERIMENTS.md)查阅。

## 当前方向与授权

按用户最新要求，V7.5研究 **Which reconstruction errors matter to the generative world model state?**：通过生成观测→策略→动作→ego/相机→下一段生成的实际反馈，判断哪些重建状态误差影响驾驶任务。停止追加长时间放大和局部定位诊断，不以证明放大作为接入闭环的前提。定义、架构与停止条件见[PROBLEM](v75/PROBLEM.md)。

单张RTX3090正式推理已获授权；慢可以接受，任何OOM立即停止，不自动降配置、重试或调研多卡。无训练、旧队列恢复或关机任务。模型与样例资源就绪，下载heartbeat已暂停。

## 已完成证据

- [单卡基线](v75/BASELINE.md)：官方单视图2B，704×1280、237帧，完整重复237/237帧一致；PyTorch峰值12.81GiB，旧整卡采样峰值17.41GiB，无OOM。另完成三个[原生开发样例](v75/NATIVE_COHORT.md)与[AV2输入桥接](v75/AV2_BRIDGE.md)。保留样例没有用于调参。
- [定位实验](v75/LOCALIZATION.md)：一个场景两seed十组，恢复条件后二维差下降，主要响应与普通投影同量级。无稳定额外放大证据，不追加长时追查。
- [自然读出](v75/NATURAL_STATE.md)：八日志筛查、四例DVGT读出保留好坏表现；2.965m候选两seed存在二维条件响应，但普通框三维更差也可生成更好的二维指标。后段固定点支持不足，不能晋级稳定米制漂移。其后四日志窗口4→3→0，真实遮挡排除，无重建/生成、不补位。
- [可见性窗口](v75/VISIBLE_COHORT.md)：4日志→8候选→2真实可测目标→2次DVGT→1参考合格目标→4组生成，全部结束。银色轿车3.423m残余未统一退化；LiDAR改善检测中心3.78→1.74px，共同点2.82→3.37px，不能声称一致修复。黑色车辆参考0.663m未过旧0.5m规则，不生成不替换。两例全局尺度均改善，保留强控制。
- 四视频各237帧全部解码；检测19/20，普通框缺一次，完整均值留空。条件刚体比较四组共同点为0，米制排名为空；请求相机不等于已验证生成相机。额外RAFT检查完成、无OOM，没有新增failure卡。
- 工程失败[V75-F01](research_failures/entries/V75-F01.md)已绕过：显式绑定底层官方pipeline输入；第三方源码未修改。人工verdict均为null。

## 当前工作与缺口

已完成三条开发任务的实际反馈：早期0bae3b5e无制动、响应小；两个有制动需求的接近任务02678d04与24642607各四组，共936帧/120次决策，均无固定参考重叠。[两任务结果](v75/APPROACH_CLOSED_LOOP.md)同时保留好坏表现，不称独立确认或跨SOTA结论。

02678d04的DVGT/尺度/LiDAR相对GT进度变化−1.347/−4.674/+1.300m；[普通直接状态控制](v75/APPROACH_STATE_CONTROL.md)为−0.871/−4.524/+0.051m。准确目标位置并未同步恢复RGB状态和动作。有限IoU关联只改善部分指标：LiDAR平均动作差1.102→0.725、最大欠制动2.420→1.678，但最大额外制动1.114→1.525m/s²，未过事前恢复条件；该变体关闭，未运行新生成，也未被后续任务采用。

另一次固定6日志18窗口得到2个记录减速、1个可见前车任务，即24642607的+6.5秒。真实RGB前车15/15、距离误差中位1.255m、动作差0.221m/s²；GT生成与逐帧参考均通过。官方DVGT残余0.974m，RGB进度差仅−0.083m；全局尺度残余3.493m、进度差−0.971m。目标LiDAR残余0.275m，平均动作差0.101→0.067，但进度差变为+0.446m，仍非一致恢复。原5点参考拒绝保留，固定补入之前两扫描后55点、目标位移0.0094m、无GT运动补偿。

第二任务的直接状态进度变化−0.452/−1.757/+0.101m；四组旧动作共468帧精确复现，再跑468帧直接状态反馈。两个接口的差值混合感知、跟踪和不同ego轨迹，不能全部归因生成器。范围仍是已知路线/标定/地形、额外GT尺寸朝向与共享非反应式轨迹；受控DVGT距离读出不等于原生端到端场景重建排名。

**决策：降低单目标初始中心平移修复的立论优先级，关闭这两个任务的追加平移、尺度、关联、seed和时长搜索。** 两个制动任务尚未确认严重、可重复、可恢复的自然重建失效；不据此启动主方法或扩大同类筛选。原48窗口0合格与新增18窗口结果分别保留，额外度量观测单列。

时间状态审计已完成，见[同一报告](v75/APPROACH_CLOSED_LOOP.md#时间状态审计近静止任务的范围边界)。官方DVGT-1输出时序点图/ego位姿，没有对象速度或未来轨迹头；OmniDreams接收外部actor轨迹。旧四组逐帧核查仅做恒定中心平移，其他时间状态仍是记录真值，不能称自然运动重建结果。两例目标在3.867s内最大参考位移只有0.179/0.159m；冻结目标姿态的CPU直接状态进度差−0.00515/+0.00119m，过去参考CV为+0.09766/−0.04249m。普通静止解释已足够，关闭这两个近静止任务的追加运动干预。

过去20capture加起点共21帧：02678d04没有通过已保存策略检测/地面接触过滤的可用目标对应（不等于原始检测器召回为零）；24642607有21/21且ID一致，但接触点速度拟合0.523m/s、参考约0.044m/s，前后半段速度向量差2.362m/s。不能将此普通读出波动归因DVGT。两例均有截止前3时刻×7视角清单，但没有新时序模型推理，不能从图像存在断言对象运动可验证。

固定运动任务窗口已结束，见[任务覆盖边界](v75/BRAKING_TASKS.md#同一固定来源的真实运动跟车资格)。复用DEV2六日志、2.5/4.5/6.5秒共18起点，协议先冻结，不看模型误差。3窗口ego速度不足、13窗口40m/路线内无前车、1窗口前车持续不足，最后1窗口仍是近静止目标，**0个真实运动跟车任务准入**。没有进入新的检测、时序重建或生成；这是有限数据的任务覆盖不足，不是模型运动能力的否定。该窗口关闭，不调阈值、补选来源或增加合成速度。

**当前重点转为普通状态构建对生成闭环的影响，已有候选信号。** 完成[形状先验审计与四段真实反馈](v75/APPROACH_CLOSED_LOOP.md#普通形状先验与生成闭环相近间距不保证相同制动)：移除目标GT尺寸/朝向，复用同一DVGT校准距离与检测框，比较固定类别尺寸、可见范围拟合。CPU五组控制全部保留，包括额外LiDAR；没有新DVGT前向。外部适配器的形状不能称为DVGT原生对象头输出。

02678d04两输入的初始近端路线间距只差3.943mm，直接状态控制进度仅差−0.000150m；经过生成RGB→检测/跟踪→IDM→ego实际反馈，可见拟合比固定类别尺寸多行进2.757m。平均自身ego参考动作误差1.562 vs 0.912m/s²，最大欠制动3.612 vs 1.906m/s²。24642607相应间距差3.691mm、CPU进度差−0.001493m、RGB进度差+0.301m，保留这个小响应。宽高与中心同时变化，不能作纯尺寸因果归因；单seed和两条曝光日志也不支持普遍性。

**普通先验只做到部分缓解，尚无完整有效主方法。** 固定类别尺寸在两例均降低平均动作误差，但第一例最大额外制动0.529→1.244m/s²；所有新分支均无固定参考重叠，没有碰撞或false-safe证据。原GT形状DVGT保留为额外信息参照。只优化近端距离不能保证生成闭环等价，值得针对这一具体候选做有限确认，而不是延长轨迹或重开中心/尺度扫描。

下一步只考虑一次预先冻结的候选确认：保持这两份合法状态构建和任务不变，固定一个额外seed，同时检查平均动作误差、欠/额外制动及执行结果；不得按结果继续挑seed。它仅验证新“形状状态导出”候选的随机性，不能重开已经关闭的单中心平移扫描，也不能称为独立新日志确认。若相同方向不能复现，则关闭该候选；即使复现，也须验证一个普通恢复输入的多指标改善，不能直接启动训练或宣称主方法。具体协议尚未建立，当前没有后续队列。

本轮1170帧CPU控制8.53秒、4段468帧/60次决策生成301.33秒，峰值12.971GiB，无OOM；新旧八段936帧按原动作精确回放，相机矩阵最大差0。首轮条件目录预检错误在模型调用前终止，失败与修复记录保留。当前GPU空闲（1MiB、0%），无训练、下载、活跃队列或电源控制。两任务新旧视频、完整曲线、时间审计及18窗口表格均在本地`outputs/V75_Multiscene_Closed_Loop/index.html`；原始数组/视频仍在各runs，完整小型证据归档至`docs/autoresearch/worldsim_v75/shape_feedback/`。人工verdict均null，未新增failure卡。

保留V7.4 [F20](research_failures/entries/V74-H2-F20.md)、[F21](research_failures/entries/V74-H2-F21.md)、[F22](research_failures/entries/V74-H2-F22.md)边界，不恢复失败的TransFuser域基线。没有有效主方法或普遍SOTA失效证据，研究目标继续推进。
