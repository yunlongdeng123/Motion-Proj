model:
  name: svd
  pretrained: stabilityai/stable-video-diffusion-img2vid-xt   # HF repo (download separately)
  parameterization: edm        # SVD uses EDM-style sigmas; projection loss uses x0-form
  sigma_floor: 1.0e-3          # raw-v 逆变换与 c_noise 的数值下限
  num_frames: 8                # must match data.num_frames (SVD-xt native is 25)
  vae_scale_factor: 8
  latent_channels: 4
  lora:
    enable: true
    rank: 16
    alpha: 16
    dropout: 0.0
    scope: temporal_only
    # 先按完整模块路径区分 temporal/spatial，再匹配这些 attention 投影
    projections: [to_q, to_k, to_v, to_out.0]
  freeze_vae: true
  gradient_checkpointing: true
  enable_xformers: true
