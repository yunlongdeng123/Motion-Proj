task_id: WS-V7-P22-AV2-TRUE-FIRST-RETURN-CORRECTION-01
hypothesis_id: H-V7-P22
runs_root: /root/autodl-tmp/runs
p2_config: configs/worldsim_v7/p2_av2_four_action_compile_v1.yaml
cohort_config: configs/worldsim_v7/av2_zero_shot_recovery_cohort_v1.json
cohort_roles: [fresh_external_confirmation]
expected_log_count: 20
legacy_attribution_run: /root/autodl-tmp/runs/worldsim_v7/WS-V7-P15-FRESH-HAZARD-ACTION-ATTRIBUTION-01/20260903T043000Z__fresh-hazard-action-raw-s0-r1
device: cuda
verdict: descriptive_consumed_av2_literal_first_return_metric_correction
claim_boundary: >-
  Correct the already consumed 20-log AV2 attribution from target-nearest
  proximity to literal minimum-positive-depth first return. Query, compiled
  surface, projection rule, cohort, actors, 0.20 m lateral tolerance, and
  0.20 m depth tolerance remain frozen. This is a necessary metric correction
  and cross-sensor diagnostic, not a fresh confirmation, fitted transfer,
  policy selection, collision certificate, or road-safety guarantee.
compiler_overrides:
  projection_output: observed_lidar_hit
attribution:
  distance_chunk_size: 1024
  ray_lateral_tolerance_m: 0.20
  depth_tolerance_m: 0.20
