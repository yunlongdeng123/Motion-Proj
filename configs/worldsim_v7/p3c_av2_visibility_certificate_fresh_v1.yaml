task_id: WS-V7-P3C-AV2-VISIBILITY-CERTIFICATE-FRESH-01
hypothesis_id: WS-V7-H-P3-003
runs_root: /root/autodl-tmp/runs
p2_config: configs/worldsim_v7/p2_av2_four_action_compile_v1.yaml
cohort_config: configs/worldsim_v7/av2_zero_shot_recovery_cohort_v1.json
cohort_roles: [fresh_external_confirmation]
expected_log_count: 20
source_cohort_previously_consumed: false
device: cuda
verdict: fresh_visibility_conditioned_ray_certificate
claim_boundary: >-
  A pre-frozen exact-once visibility-conditioned audit on 20 previously unused AV2 logs.
  Target-only LiDAR rays partition output into observed-free contradiction, observed-hit support,
  and occluded/off-ray UNKNOWN. UNKNOWN is never relabelled as free or occupied. No AV2 fit,
  calibration, threshold selection, scene deletion, or operator change is permitted. The certificate
  is limited to the observed ray set and is not a rendering guarantee, collision certificate,
  planning result, closed-loop result, or road-safety guarantee.
compiler_overrides:
  projection_output: observed_lidar_hit
visibility_certificate:
  distance_chunk_size: 1024
  ray_lateral_tolerance_m: 0.20
  depth_tolerance_m: 0.20
